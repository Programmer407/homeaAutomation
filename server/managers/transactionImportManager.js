/*
// libs
import TransactionImportType from '../models/TransactionImportType'

export const findTrxImportTypeById = (id) =>
  TransactionImportType.findOne(Object.assign({
    where: {
      id
    }
  }))
  .then(obj => {
    return obj
  })
*/
