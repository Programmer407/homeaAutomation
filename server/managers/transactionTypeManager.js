/*
// libs
import TransactionType from '../models/TransactionType'

export const findTransactionTypeById = (id) =>
  TransactionType.findOne(Object.assign({
    where: {
      id
    }
  }))
  .then(obj => {
    return obj
  })

export const findTransactionTypeByName = (typeName) =>
  TransactionType.findOne(Object.assign({
    where: {
      typeName
    }
  }))
  .then(obj => {
    return obj
  })
*/
